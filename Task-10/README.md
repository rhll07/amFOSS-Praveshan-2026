# Task-10: Surprise Us (Password Generator)

A small Python program that generates a random password of a user-specified length.

## How It Works

The program asks the user for the desired password length and then generates a password using:

- Uppercase letters
- Lowercase letters
- Numbers
- Special characters

It uses Python's `secrets` module to select characters randomly.

## Running

```bash
python3 password.py